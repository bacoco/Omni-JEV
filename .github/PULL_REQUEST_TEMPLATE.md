## Issue and scope

Related issue: #

Describe the acceptance criteria implemented. Use `Closes #N` only when the full
issue is covered; use `Refs #N` for partial work. Do not auto-merge research changes.

## Changes

Explain the implementation and which upstream components are reused.

## Validation

- Commands and observed results:
- Local environment / pinned model revisions where applicable:
- Tests not run and reasons:

Synthetic fixtures and mocked calls are not model evaluation. Include data split,
compute cost and limitations when reporting an actual model experiment.

## Remaining work

List unmet criteria, dependency issues, and explicit compatibility limitations.
